"""
Candidates router — Phase 1 stub.
Full implementation arrives in Phase 3 (Semantic Scoring Service).
"""

from fastapi import APIRouter

router = APIRouter(prefix="/candidates", tags=["candidates"])
